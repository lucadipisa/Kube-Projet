apiVersion: apps/v1
kind: Deployment
metadata:
  name: flask-deployment-ldp
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flask-ldp  
  template:
    metadata:
      labels:
        app: flask-ldp 
    spec:
      containers:
      - name: flask-app
        image: your-flask-image
        ports:
        - containerPort: 5000
