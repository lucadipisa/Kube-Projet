apiVersion: apps/v1
kind: ReplicaSet
metadata:
  name: flask-deployment
spec:
  replicas: 3
  selector:
    matchLabels:
      app: flask
  template:
    metadata:
      labels:
        app: flask
    spec:
      containers:
      - name: flask-container
        image: axos15/flask-actions-project:latest
        ports:
        - containerPort: 7897